import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        borderColor: "#333333",
        borderTopWidth: 1,
        paddingVertical: 48,
        alignItems: "center",
    },
    title: {
        fontWeight: "bold",
        marginTop: 16,
    },
    text: {
        fontSize: 14,
        color: "#808080",
    }
})